# Windows batch scripts must use CRLF. cmd.exe's goto/label parser desyncs on
# LF-only files, corrupting execution (set/goto break, %~dp0 comes back empty).
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell scripts must stay LF, even when checked out on Windows.
*.sh text eol=lf
